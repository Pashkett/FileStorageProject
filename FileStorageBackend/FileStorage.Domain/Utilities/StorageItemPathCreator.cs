using FileStorage.Data.Models;
using Microsoft.Extensions.Configuration;
using System;
using System.Collections.Generic;
using System.IO;
using System.Text;

namespace FileStorage.Domain.Utilities
{
    public static class StorageItemPathCreator
    {
        private static string FolderPathSetter(StorageItem folder, IConfiguration configuration)
        {
            string path = string.Empty;

            return path;
        }

    }
}
